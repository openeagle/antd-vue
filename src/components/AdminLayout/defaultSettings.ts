import { Settings } from './typings';

const defaultSettings: Settings = {
  title: '',
  layout: 'top',
  fixedHeader: true,
  fixSiderbar: true,
  headerTheme: 'dark',
  navTheme: 'dark',
  contentWidth: 'Fluid',
  headerHeight: 48,
  siderWidth: 208,
  routerTabs: false,
  topTabsIcon: false,
  iconScriptUrl: ''
};

export default defaultSettings;
